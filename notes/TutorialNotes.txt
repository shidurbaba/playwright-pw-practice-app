Promise is a type of JavaScript object that represents the eventual completion (or failure) of an asynchronous operation and its resulting value.
Here, we are using the 'await' keyword to wait for the page to load completely before proceeding with any further actions.
await using the keyword asynchronously waits for the Promise returned by page.goto() to resolve.


So playwright has four hooks that you can use 
1. before each 
2. after each 
3. before all
4. after all
These hooks are useful for setting up preconditions and cleaning up after tests.

When you group your test by the test suites using describe, you can skip them independently by using the .skip method.
For example:
describe.skip('My Test Suite', () => {
  // tests go here
});
This will skip all tests within the 'My Test Suite' describe block.

In Playwright, you can also use test.only to run a specific test or test suite exclusively. This is useful for debugging or focusing on a particular test without running the entire suite.
For example:
test.only('My Exclusive Test', async ({ page }) => {
  // test code goes here
});

    //1. Locate by tag name - locate by tag name will return all the web elements with the given tag name
    await page.locator('input').first().click(); //this will click on the first input field on the page

    //2. Locate by id - locate by id will return a single web element with the given id
    await page.locator('#inputEmail1').click();

    //3. Locate by class value - locate by class value will return all web elements with the given class value
    page.locator('.shape-rectangle');

    //4. Locate by attribute - you can locate elements by attribute using
     playwright will all web elements with attribute value [] 
    page.locator('[placeholder="Email"]');

    //5. By Full Class Value - locate by full class value will return all web elements with the given full class value
    page.locator('[class="input-full-width size-medium status-basic shape-rectangle nb-transition"]');

    //6. Combine different selectors - you can combine different selectors to narrow down your search
    page.locator('input[placeholder="Email"].shape-rectangle');

    //7. Xpath locator (No recommend to use it) - you can use xpath to locate elements as well
    page.locator('//input[@id="inputEmail1"]');

    //8. Partial Text Match - you can locate elements by partial text match using :text() selector
    page.locator(':text("Using")')

    //9. Exact Text Match - you can locate elements by exact text match using :text-is() selector
    page.locator(':text-is("Using the Grid")')


    first() - This method is used to select the first element from a list of matched elements.
    last() - This method is used to select the last element from a list of matched elements
    nth(index) - This method is used to select the element at the specified index from a list of matched elements. The index is zero-based.

    has - This method is used to filter elements that contain a specific child element.
    For example:
    await page.locator('nb-card', {has: page.locator('#inputEmail1')} ).getByRole('textbox', {name: 'Email'}).click();

    hasText - This method is used to filter elements that contain specific text.
    For example:
    await page.locator('nb-card', {hasText: 'Using the Grid'}).getByRole('textbox', {name: 'Email'}).click();

Radio Button
 await usingTheGridForm.getByLabel('Option 1').check({force: true})
The check() method is used to select a radio button or checkbox.
The {force: true} option is used to force the action even if the element is not visible or not enabled.
Example class='native-input visually-hidden' is a common class for hidden radio buttons or checkboxes, and using {force: true} allows you to interact with them regardless of their visibility.
     
await usingTheGridForm.getByRole('radio', {name: "Option 2"}).check({force: true}) //Another option to check the radiobutton

How to validate if our selection was successful?
const radioStatus = await usingTheGridForm.getByRole('radio', {name: "Option 2"}).isChecked()
isChecked() method is used to check if a radio button or checkbox is selected.
expect(radioStatus).toBeTruthy();
toBeTruthy() method is used to assert that the value is true.

//How to validate using locator?
await expect(usingTheGridForm.getByRole('radio', {name: "Option 2"})).toBeChecked()
toBeChecked() method is used to assert that the radio button or checkbox is selected.

//How to check if Option 1 is not checked without creating a new constant?
expect(await usingTheGridForm.getByRole('radio', {name: "Option 1"}).isChecked()).toBeFalsy()
tobeFalsy() method is used to assert that the value is false.

Checkbox
wait page.getByRole('checkbox', { name: "Hide on click" }).check({ force: true })
//check () - if the checkbox already checked it will not unselect this checkbox, while click command just performs the click and doesn't validate.
await page.getByRole('checkbox', { name: "Hide on click" }).uncheck({ force: true })
await page.getByRole('checkbox', { name: "Prevent arising of duplicate toast" }).check({ force: true })

const allBoxes = page.getByRole('checkbox')

for (const box of await allBoxes.all()) {
  .all - This method is used to retrieve all elements that match the locator and returns them as an array of ElementHandle objects.
    await box.check({ force: true })
    expect(await box.isChecked()).toBeTruthy()

    //await box.uncheck({force: true})
    //expect(await box.isChecked()).toBeFalsy()
    isChecked() method is used to check if a checkbox is selected.
    toBeTruthy() method is used to assert that the value is true.
        }

Lists and Dropdowns
   page.getByRole('list') - This method is used to locate an element with the role of 'list'. It will return the first element that matches this role.
   page.getByRole('listitem') - This method is used to locate elements with the role of 'listitem'. It will return all elements that match this role.

        const colors = { - const colors is an object that contains key-value pairs where the keys are the names of the color options in the dropdown menu and the values are the corresponding RGB color values. This object is used to store the expected background color values for each color option in the dropdown menu.
            "Light": "rgb(255, 255, 255)",
            "Dark": "rgb(34, 43, 69)",
            "Cosmic": "rgb(50, 50, 89)",
            "Corporate": "rgb(255, 255, 255)"
        }

        //Create for in loop
        await dropdownMenu.click(); //open the dropdown
        for (const color in colors) {
          // in - This is a JavaScript keyword used to iterate over the properties of an object. In this case, it is used to iterate over the keys of the 'colors' object, which represent the different color options in the dropdown menu.
            await optionList.filter({ hasText: color }).click() - This line of code is used to select a specific option from the dropdown menu based on the color name. The filter() method is used to narrow down the list of options to those that contain the specified text (color), and then click() is called to select that option.
            await expect(header).toHaveCSS('background-color', colors[color]) - This line of code is used to assert that the background color of the header element matches the expected color value from the 'colors' object. The toHaveCSS() method is used to check if the header element has the specified CSS property ('background-color') with the expected value (colors[color]).
            if(color != 'Corporate')
            await dropdownMenu.click(); //open the dropdown again

        }

Tooltips
To validate the tooltip text, you can use the getByRole() method to locate the element that triggers the tooltip and then use the hover() method to simulate hovering over that element. 
After that, you can use the getByRole() method again to locate the tooltip element and assert its text content.

  const tooltipCard = page.locator('nb-card',{hasText: "Tooltip Placements"})
  await tooltipCard.getByRole('button', {name: "Top"}).hover()